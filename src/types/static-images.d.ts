// Static image modules. Next declares these in the generated, gitignored
// next-env.d.ts, so a fresh checkout has none and CI type-checks before it builds.
/// <reference types="next/image-types/global" />

export {};
