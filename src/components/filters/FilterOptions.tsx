// FilterOptions.tsx
import styles from './FilterOptions.module.css';
import React from 'react';

const FilterOptions: React.FC = () => {
    return (
        <div className={styles.container}>
            {/* Origin toggle removed */}
        </div>
    );
};

export default FilterOptions;
